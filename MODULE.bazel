module(
    name = "osqp-eigen",
    version = "0.11.0",
    bazel_compatibility = [">=7.2.1"]
)

# These dependencies can only be used from bazel-central-registry
bazel_dep(name = "platforms", version = "1.0.0")
bazel_dep(name = "rules_cc", version = "0.2.17")

# These dependencies can be overriden using conda ones
bazel_dep(name = "eigen", version = "3.4.0.bcr.3")
bazel_dep(name = "osqp", version = "1.0.0.bcr.2")
bazel_dep(name = "catch2", version = "3.8.0")

